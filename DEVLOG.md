# BodyBot 개발 로그 — 바이브 코딩 세션 기록

> 실시간 신체 모션 캡처 → 3D 캐릭터 미러링 웹앱  
> 브랜치: `claude/motion-capture-3d-character-1lXSa`  
> 개발 방식: 바이브 코딩 — 의도와 느낌을 전달하면 AI가 구현

---

## 커밋 히스토리

| # | 커밋 | 내용 |
|---|------|------|
| 1 | `ed677bb` | 초기 앱 생성 |
| 2 | `1076830` | 뼈 회전 로직 수정 + character.glb 기본 로드 |
| 3 | `82b7951` | 뼈 이름 자동 감지 |
| 4 | `d80b2a9` | 드래그앤드롭 + 파일 선택으로 모델 로드 방식 교체 |
| 5 | `59ff2df` | 줌·캐릭터 크기 슬라이더 범위 확장 |
| 6 | `e6e0857` | 힙 yaw(좌우 돌기) 추가 |
| 7 | `9455c18` | 몸 기울기(roll/pitch) 수정 — 전체 3D 방향 반영 |
| 8 | `8b45d6e` | 손가락 인식 추가 (MediaPipe Holistic) |
| 9 | `8606ae7` | 배경 밝기 조정 |

---

## 세션 상세 기록

---

### 1. 앱 전체를 만들어줘

**프롬프트**
> 웹캠으로 몸 움직임을 실시간으로 추적해서 브라우저에서 3D 캐릭터가 같은 포즈를 따라하는 웹 앱을 만들어줘.
> 설치 없이 브라우저에서 바로 돼야 하고, 파일 하나로 끝내줘.
> 캐릭터 모델 없어도 일단 돌아가야 해.

**AI가 한 것**
- `index.html` 단일 파일로 전체 앱 구현 (npm/빌드 도구 없음)
- 웹캠 영상을 MediaPipe로 분석해 몸의 33개 관절 위치를 실시간 추출
- Three.js로 3D 씬 구성 (카메라, 조명, 바닥, 그림자)
- GLB 모델이 없을 때 자동으로 박스 조각으로 만든 임시 캐릭터(박스맨) 생성
- 웹캠 화면에 관절·뼈 연결선 오버레이 실시간 드로잉
- 스무딩 슬라이더, 크기 조절, 배경 전환 등 UI 컨트롤 패널 구성

**결과**
```
index.html, README.md 생성
→ 브라우저에서 즉시 실행, 박스맨이 팔다리를 따라 움직임
```

---

### 2. 추적 정확도 개선 + 기본 모델 교체

**프롬프트**
> 추적이 제대로 안 돼. 캐릭터가 내 움직임을 따라오는 느낌이 전혀 없고,
> 팔이랑 다리가 엉뚱한 방향으로 꺾여. 회전 로직에 뭔가 근본적으로 잘못된 것 같아.
> 고치고, 기본 모델도 character.glb로 바꿔줘.

**AI가 한 것**
- 뼈 회전 계산의 근본 오류 수정
  - 기존: 월드 방향 벡터를 그냥 뼈의 로컬 회전으로 쑤셔넣음
  - 수정: 부모 뼈의 월드 변환을 역산해서 로컬 공간으로 변환한 뒤 회전 적용
- 모델 로드 시 각 뼈의 기본 방향(레스트 포즈)을 자동 저장해두고, 이후 회전은 그 위에 누적
- 기본 모델을 character.glb로 변경, 없으면 박스맨으로 폴백

**결과**
```
팔/다리/척추 방향 추적 정확도 개선
character.glb 있으면 자동 로드
```

---

### 3. 다른 GLB 모델도 뼈대 인식이 되어야 해

**프롬프트**
> Mixamo에서 받은 다른 GLB 모델 넣어봤는데 뼈대를 아예 못 잡아.
> 어떤 GLB 파일을 쓰든 자동으로 뼈대를 찾아서 연결해줘.
> 익스포터마다 이름 규칙이 달라도 동작해야 해.

**AI가 한 것**
- GLB 파일을 불러와도 뼈를 못 찾는 원인 파악
  - GLB 익스포터마다 뼈를 `Bone` 타입이 아닌 일반 `Object3D`로 내보내는 경우가 있음 → 기존 코드가 통째로 무시
  - 뼈 이름이 `mixamorig*` 형식이 아니면 매핑 자체가 안 됨
- 모든 이름 있는 오브젝트를 수집하도록 수정
- 뼈 이름 자동 해석 함수 추가: 정확한 이름 → 대소문자 무시 부분 매칭 순으로 탐색
- Mixamo, Reallusion CC, 일반 명명 규칙 등 다양한 포맷 지원
- 화면에 토스트 메시지로 뼈 매핑 결과 표시 (`✓ 뼈대 11개 모두 매핑됨` 등)

**결과**
```
Mixamo 외 다양한 GLB 모델 자동 인식
뼈 매핑 성공/실패 여부를 화면에서 바로 확인 가능
```

---

### 4. 파일 경로 방식 버리고 브라우저에서 직접 로드하게 해줘

**프롬프트**
> 폴더 만들어서 파일 넣어도 여전히 인식이 안 돼.
> 경로로 파일 접근하는 방식 자체가 문제인 것 같아.
> 그냥 파일을 브라우저 화면에 드래그 앤 드롭하면 바로 로드되게 해줘.

**AI가 한 것**
- 구조적 문제 파악: 로컬 서버가 파일을 직접 서빙하지 않으면 경로로 파일에 접근 자체가 불가
- 파일 경로 방식을 완전히 버리고 브라우저 메모리로 직접 읽는 방식으로 전환
- **드래그 앤 드롭** 추가: GLB 파일을 3D 화면 위에 끌어다 놓으면 즉시 로드, 드롭 시 네온 테두리 하이라이트
- **파일 선택 버튼** 추가: `📂 GLB 불러오기` 버튼으로 탐색기에서 선택
- models/ 폴더, 서버 경로 의존성 완전 제거

**결과**
```
폴더 구조, 서버 설정 전혀 불필요
GLB 파일을 브라우저에 드래그 한 번으로 즉시 로드
```

---

### 5. 캐릭터 크기 조절 범위 넓혀줘

**프롬프트**
> 캐릭터가 화면을 너무 꽉 채워서 전체 모습을 보기가 힘들어.
> 슬라이더로 훨씬 작게 줄일 수 있게 해주거나, 스크롤로 카메라가 훨씬 멀리 빠질 수 있게 해줘.

**AI가 한 것**
- 스크롤 줌 최대 거리를 12 → 50으로 확장 (훨씬 멀리 빠질 수 있음)
- 크기 슬라이더 범위를 0.5~3 → 0.05~5로 확장 (훨씬 작게, 훨씬 크게)

**결과**
```
스크롤로 멀리 줌아웃 가능
슬라이더로 캐릭터를 거의 점 크기까지 축소 가능
```

---

### 6. 몸통 회전도 캐릭터에 반영해줘

**프롬프트**
> 지금 팔다리만 따라오고 몸통 회전이 전혀 없어.
> 내가 몸을 왼쪽이나 오른쪽으로 돌면 캐릭터도 같이 돌아야 해.
> 몸통이 어느 방향을 보는지도 반영해줘.

**AI가 한 것**
- 힙(Hips) 뼈에 좌우 회전(yaw) 추가
- 어깨 축과 힙 축을 XZ 평면에 투영해 몸이 얼마나 돌아있는지 각도 계산
- 두 벡터 평균을 내서 노이즈 줄임
- 미러뷰에 맞게 방향 보정

**결과**
```
몸을 왼쪽/오른쪽으로 돌면 캐릭터도 같이 회전
```

---

### 7. 몸 기울기까지 3D 전방향으로 다 잡아줘

**프롬프트**
> 좌우 회전은 됐는데 기울기가 여전히 안 돼.
> 내가 옆으로 기울거나 앞뒤로 구부리면 캐릭터도 그대로 따라와야 해.
> 회전, 기울기, 앞뒤 피치 전부 3D로 한 번에 잡아줘.

**AI가 한 것**
- 버그 2개 동시 발견 및 수정
  1. `poseWorldLandmarks`가 미터 단위인데 0~1 범위 정규화 공식을 적용하고 있었음 → 기울기 계산 결과 완전히 엉망
  2. 힙에 좌우 돌기(yaw)만 적용하고 기울기(roll, pitch)는 누락
- 힙 회전 방식을 완전히 교체
  - 어깨 중점-힙 중점으로 몸의 위 방향 벡터 계산
  - 어깨 폭으로 몸의 오른쪽 방향 벡터 계산
  - 두 벡터의 외적으로 앞 방향까지 구해서 3D 회전 행렬 완성
  - yaw + roll + pitch 한 번에 처리
- 힙 업데이트 직후 월드 행렬 즉시 갱신 → 척추/팔/다리가 새 힙 기준으로 같은 프레임 내 계산

**결과**
```
좌우 기울기(roll) + 좌우 돌기(yaw) + 전후 기울기(pitch) 모두 반영
몸을 어느 방향으로 비틀어도 캐릭터가 따라옴
```

---

### 8. 손가락 움직임까지 따라하게 해줘

**프롬프트**
> 손동작까지 미러링하고 싶어.
> 주먹 쥐거나 손가락을 하나씩 펼칠 때 캐릭터 손가락도 같이 움직여야 해.
> 각 마디마다 방향이 다 잡혀야 자연스러울 것 같아.

**AI가 한 것**
- MediaPipe Pose → Holistic으로 교체 (포즈 + 양손 손가락 동시 인식)
- 손당 21개 관절 포인트 활용, 각 손가락 마디(3마디 × 5손가락 × 양손 = 30개) 뼈 매핑 추가
- 각 마디 방향 벡터를 뼈에 실시간 적용
- 웹캠 오버레이에 손가락 관절 라인 추가 (왼손 파란색, 오른손 주황색)

**결과**
```
손가락 굽힘/펴기, 각 마디 방향 실시간 반영
GLB 캐릭터에 손가락 뼈가 있어야 동작 (Mixamo Y Bot 등 포함)
```

---

### 9. 배경 톤 조금 올려줘

**프롬프트**
> 배경이 너무 어두워서 캐릭터가 묻혀 보여.
> 완전 밝게 말고, 지금 다크 톤은 유지하면서 조금만 올려줘.

**AI가 한 것**
- 배경, 안개, 바닥, 그리드를 모두 한 톤 밝게 조정
- 다크 테마 느낌은 유지하되 너무 검지 않게

**결과**

| 항목 | 전 | 후 |
|------|----|----|
| 배경 | `#0a0a1a` 거의 검정 | `#2a2a3a` 어두운 보라-회색 |
| 바닥 | `#050510` | `#1a1a2e` |
| 그리드 | `#111133` | `#3a3a5a` |

---

## 최종 앱 기능 요약

### 인식 범위
| 부위 | 본 |
|------|-----|
| 몸통 전체 방향 (yaw/roll/pitch) | Hips |
| 상체 기울기 | Spine |
| 고개 | Neck |
| 팔 상완/전완 | LeftArm, LeftForeArm 외 |
| 다리 대퇴/하퇴 | LeftUpLeg, LeftLeg 외 |
| 손가락 각 마디 | HandThumb1~3, HandIndex1~3 외 |

### 모델 로드
- GLB 파일을 3D 뷰에 **드래그 앤 드롭**
- `📂 GLB 불러오기` 버튼으로 파일 탐색기에서 선택
- 기본: 박스맨 (파일 없어도 즉시 실행)

### 컨트롤
| 컨트롤 | 기능 |
|--------|------|
| 스무딩 슬라이더 | 0 = 즉각 반응 / 0.95 = 부드럽게 |
| 캐릭터 크기 슬라이더 | 0.05 ~ 5배 |
| 마우스 드래그 | 3D 뷰 회전 |
| 스크롤 휠 | 줌 (거리 0.5 ~ 50) |
| 포즈 리셋 버튼 | 모든 본을 기본 자세로 복원 |
| 배경 드롭다운 | 어둡게 / 밝게 전환 |

---

## 기술 스택

```
Three.js r158 CDN          — 3D 렌더링, 뼈대 제어
MediaPipe Holistic 0.5     — 포즈 + 양손 손가락 실시간 인식
GLTFLoader                 — GLB 모델 ArrayBuffer 파싱
HTML/CSS/JS 단일 파일       — 빌드 도구 없음, CDN만 사용
```
